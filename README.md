SETUP
<hr>
***Notes!***
TOKEN_SECRET can get from that for generating random string 32 char
    
```yaml
openssl rand -base64 32
```

Run Container

```yaml
./deploy.sh
```

Remove Container

```yaml
./down.sh
```